{
  "version": 2,
  "name": "figma-ui",
  "builds": [
    { "src": "package.json", "use": "@now/static-build", "config": { "distDir": ".out" } }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "routes": [
    { "src": "^/(.*)", "dest": "/$1" }
  ]
}
